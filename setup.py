from setuptools import setup, find_packages

setup(
    name="delegent",
    version="0.1",
    description="Delegent is a modular and extensible framework for intelligent task decomposition and delegation in multi-agent workflows. ",
    author="Soharab Hossain Shaikh",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
